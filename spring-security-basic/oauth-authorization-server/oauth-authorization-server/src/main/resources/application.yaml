server:
  port: 9000

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/RegistrationSystem
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

#  security:
#    oauth2:
#      authorization-server:
#        issuer-uri: http://auth-server:9000

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect